// Parent calendar page — stub.
// Will be filled in with availability-marking UI in a later feature.
// The parent's marked availability will eventually be matched against
// tutor availability to simplify session scheduling.
export default function ParentCalendarPage() {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold tracking-tight text-zinc-900 dark:text-zinc-50 mb-2">
        Calendar
      </h1>
      <p className="text-zinc-500 dark:text-zinc-400">
        Your availability calendar — coming soon.
      </p>
    </div>
  );
}
